@using Conesoft.Hosting

@inject HostEnvironment environment

@ChildContent.Invoke(environment)

@code {
    [Parameter]
    public RenderFragment<HostEnvironment> ChildContent { get; set; } = default!;
}